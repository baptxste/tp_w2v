class Dataloader:
    def __init__(self, path, L, k) :
        self.path = path
        self.L = L
        self.k = k
    def cpos(self):
        file = open(path)
        
        for k in range self.file[L,:-L]:
